---
name: phase_registry
type: context
---

# Phase Registry

| Phase | Date | Status | Files Touched (key) | Key Decisions |
|-------|------|--------|---------------------|---------------|
| P3 | — | Merged | `pubspec.yaml`, `lib/main.dart`, project scaffold | Flutter project bootstrapped; Clean Architecture folder structure established |
| P4 | — | Merged | `lib/features/game/domain/` | Graph-based domain: Arrow, Node, Edge, GameSession, MovementResolver; no matrix/grid |
| P5 | — | Merged | `assets/levels/manual_levels.json`, `lib/features/game/infrastructure/` | Manual JSON levels as authoritative source; LevelDefinitionValidator introduced |
| P6 | — | Merged | `lib/features/game/presentation/` | Playable UI: GraphBoard painter, tap-to-move, victory screen |
| P7 | — | Merged | `lib/features/progress/`, `lib/features/settings/`, audio assets | Local progress + unlocking (SharedPreferences); settings screen; audio foundation |
| P8 | — | Merged | `lib/core/network/`, `lib/features/auth/`, `lib/features/leaderboard/`, `lib/features/progress/infrastructure/` | HTTP integration; optional auth; progress sync with merge policy; leaderboard submission; backend non-blocking |
| P9 | — | Done | `lib/features/game/domain/`, `lib/features/game/application/`, `lib/features/game/presentation/`, `assets/levels/manual_levels.json` | Full-exit rule; whole-arrow collision; atomic rollback; lives system (3 lives/−1 per 2 mistakes/game-over at 6); exit + collision animations; level redesign (15 levels); 92 tests |
| P10 | — | Done | `docs/LEVEL_AUTHORING.md`, `tool/gen_levels.js`, `assets/levels/manual_levels.json`, `lib/features/game/presentation/widgets/graph_board.dart` | Level authoring guide; greedy solvability solver; density tuning; pan/zoom (`InteractiveViewer`); connected-traversal-graph requirement; 95 tests |
| P11 | — | Done | `lib/features/game/domain/arrow_path.dart`, `lib/features/game/domain/level_definition_validator.dart`, `lib/features/game/presentation/widgets/graph_board_painter.dart`, `lib/features/game/presentation/widgets/graph_board_hit_tester.dart` | `orderedNodeIds` added to ArrowPath; smooth polyline rendering with `StrokeJoin.round`; hit-test uses ordered nodes; 107 tests |
| P11b | — | Done | `tool/gen_levels.js`, `assets/levels/manual_levels.json`, `test/features/game/infrastructure/manual_levels_test.dart` | Random level generator rewrite (sparse graph + DFS partition); comb pattern for bent-arrow tiers; 108 tests |
| P12 Prep | 2026-06-19 | Done | `harness/` (12 new files) | Development harness created; no Dart code changed; baseline 108 tests unchanged |
| P12 | 2026-06-19 | Done | `lib/features/game/application/movement_resolver.dart`, `lib/features/game/domain/board_graph.dart`, `tool/gen_levels.js`, `assets/levels/manual_levels.json`, `test/features/game/application/bent_arrow_test.dart`, `test/features/game/presentation/playable_game_ui_test.dart` | Collision fix: sweep now steps by coordinate (not graph edge) so sparse-graph levels detect blockers at adjacent coords without a connecting edge; JS solver updated symmetrically; levels regenerated; comb fallback density parameters corrected; 109 tests |
| P12.1 | 2026-06-19 | Done | `lib/features/game/application/movement_resolver.dart`, `test/features/game/application/bent_arrow_test.dart` (+ `tool/gen_levels.js`, `assets/levels/manual_levels.json` if needed) | Scope correction on P12: only the HEAD (`endNodeId`) sweeps/collides; body nodes follow the head's path with no independent collision check. Arrow is a rigid piece — head leads, body follows; head blocked → whole-arrow atomic rollback |
| P13 | 2026-06-19 | Complete | `lib/features/game/presentation/widgets/graph_board_painter.dart`, `lib/features/game/presentation/widgets/graph_board.dart` | Path-following exit animation (train on tracks): arc-length track sampling — precompute cumulative pixel distances tail→head; each node at index `i` from head advances `totalDistance*localT` along the polyline from its starting position, rounding bends, then continues in exit direction past the head; head fades first; 109/109 green |
| P13.1 | 2026-06-19 | Complete | `tool/gen_levels.js`, `assets/levels/manual_levels.json` | Level direction variety (generator-only): fixed `canExit` to sweep from head only; removed horizontal bias from `partitionNodes`; added variety check (hasVertical + maxDirFrac ≤ 60%); all 15 levels from random partition algorithm (no deterministic fallback); raised hard-tier retry budget 200 → 3000; `buildCombFallback` marked dead code; 109 tests |
| P13.2 | 2026-06-20 | Done | `tool/gen_levels.js`, `assets/levels/manual_levels.json`, `test/features/game/infrastructure/manual_levels_test.dart`, `test/features/game/presentation/playable_game_ui_test.dart`, `test/widget_test.dart` | Level name simplification: all 15 `LEVEL_DEFS` names changed to `'Level N'`; regenerated JSON (generation algorithm unchanged); updated 4 name assertions across 3 test files; 109 tests green, analyze clean |
| P14.1 | 2026-06-22 | COMPLETE | `lib/features/game/domain/level_definition_validator.dart`, `tool/gen_levels.js`, `test/features/game/domain/level_definition_validator_test.dart`, `assets/levels/manual_levels.json` | **Bug (clarified):** self-intersection — Level 12 arrow a15 was a U-spiral whose head pointed right back into its own tail (n3_3 = startNodeId at x=3,y=3, one step right of head at x=2,y=3). Not a mathematical cycle (5 edges, 6 nodes) but visually appears as a closed loop; exits confusingly by sweeping through own body. **Fixes:** (1) `hasSelfIntersectingArrow(dj)` added to generator — rejects any level where an arrow's head sweep (coordinate-based) hits a node in its own covered set (excluding head); wired into `generateLevel` after gap-exit check. (2) Self-intersection check added to `structureErrors` for `--validate-only`. (3) Self-intersection check added to `LevelDefinitionValidator.validate()` using pre-built `coordToNodeId` map; throws `LevelDefinitionException` with arrow id and first hit node. (4) New Dart test `should_reject_arrow_with_self_intersecting_sweep`. (5) Levels regenerated; all 15 clean (`--validate-only` ALL VALID: true). 122 tests green, `flutter analyze` no issues. |
| P15 | 2026-06-22/23 | Done (manual device validation pending) | `lib/features/audio/infrastructure/audio_manager.dart` (new), `lib/features/audio/infrastructure/audio_dependencies.dart` (deleted), `lib/features/audio/infrastructure/audio_players_audio_port.dart`, `lib/features/audio/infrastructure/audio_players_music_port.dart`, `lib/features/game/presentation/game_screen.dart`, `assets/audio/victory.mp3` | **Corrects the P14 Task A audit** (architecture-compliant ≠ runtime-correct). User-reported real-device bugs: crashes, music/SFX silencing each other, crackling, sped-up victory/defeat. Root causes: (1) `AudioDependencies` built fresh `AudioPlayer`s per `GameScreen` mount, never disposed → leak/crash. (2) Music port had no `AudioContext`, didn't negotiate ducking with SFX's `gainTransientMayDuck` → mutual silencing. (3) Music volume `1.1` (>1.0) passed unclamped into native `setVolume` → clipping. (4) One shared `AudioPlayer` raced across all 4 SFX events (`stop()`+`play()`); `victory.mp3` also encoded at 48kHz vs 44.1kHz for the rest. **Fixes:** app-lifetime `AudioManager` singleton (replaces per-screen factory) with reference-counted `startMusic`/`stopMusic` (needed because `pushReplacementNamed` on Next Level disposes the old screen while mounting a new one — both call the singleton, refcounting prevents the old screen's stop from killing music the new screen just started); SFX pool of 3 players instead of 1 shared; explicit, coordinated `AudioContext` on both ports; volume clamped to 1.0 then tuned to 0.6 (quieter than SFX per user request); `victory.mp3` re-encoded to 44100 Hz via `ffmpeg` (installed through `scoop` after `choco` failed on a permissions lock). 122 tests green (no new tests — root causes are native-plugin/device behaviors fakes can't exercise), `flutter analyze` clean, `gen_levels.js --validate-only` unaffected (no level files touched). |
| P14 | 2026-06-22 | COMPLETE | `tool/gen_levels.js`, `lib/features/game/domain/level_definition_validator.dart`, `test/features/game/domain/level_definition_validator_test.dart`, `test/features/game/game_test_fixtures.dart`, `test/features/game/application/exit_attempt_resolver_test.dart`, `test/features/game/application/move_arrow_use_case_test.dart`, `test/features/game/presentation/game_screen_controller_test.dart`, `test/features/game/infrastructure/manual_levels_test.dart`, `assets/levels/manual_levels.json` | **Task A (audit):** Audio/music/localization code already fully Clean Architecture compliant; all gaps already implemented — no refactor needed. **Task B (collision validator):** Added `noSharedNodes()` and `hasInteriorGapExit()` + `flipInteriorGapArrows()` to `gen_levels.js`; added node/edge disjointness enforcement to Dart `LevelDefinitionValidator`; `collisionDefinition()` helper; 2 new validator tests. **Task B deep runtime audit:** Root cause found — hard-level boundary-node removal created interior coordinate gaps in sweep paths; arrows exiting through gaps appeared to escape past visible blockers. Fix: `flipInteriorGapArrows()` reverses affected arrows (computing new direction from first-step of original path, not `OPP[direction]`, to preserve head-behind invariant for bent arrows); `hasInteriorGapExit()` added as generation check and `validateAll` column. All 15 levels regenerated; `gapExit=-` for all. Added `should_have_no_interior_gap_exits` Dart test to `manual_levels_test.dart`. `MAX_RETRIES.hard` raised 3000 → 8000. `MovementResolver`, `BoardGraph`, and `LevelDefinitionValidator` unchanged (runtime resolver is correct). 119 tests green, analyze clean |
| P16 | 2026-06-23 | Done (manual emulator validation pending) | `tool/gen_levels.js`, `assets/levels/manual_levels.json`, `test/features/game/infrastructure/manual_levels_test.dart`, `docs/LEVEL_AUTHORING.md` | Extended 15→20 levels with a new figure-level sub-tier (16–20): heart, diamond, club, spade, crown — fixed shape silhouettes instead of random rectangles (`AppConfig.manualLevelCount=20` and its 3 call sites were already wired in by a prior session; this phase supplied the actual level content). Added `heartNodeSet`/`diamondNodeSet`/`clubNodeSet`/`spadeNodeSet`/`crownNodeSet` (continuous formula rasterized to a grid, `keepLargestComponent` safety net), `generateFigureLevel` (reuses `partitionNodes`/`Builder`/`weave`+`weaveH`; stricter all-four-directions check; **does not** apply `hasInteriorGapExit`/`flipInteriorGapArrows` — that check is for accidental holes in nearly-rectangular boards, not a deliberately concave silhouette, and rejected ~100% of valid partitions when tried), and a new `--generate-figures` CLI mode that regenerates only 16–20, carrying 1–15 through byte-identical. Generalized the two places that hardcoded `15` (`--validate-only`'s count gate → contiguous-numbering check; the hard-tier difficulty-progression check → "every number ≥11 is hard" instead of "11–15 are hard"). Iterated 3 times on user feedback: (1) initial spade/star arrow density was too high ("too little/small arrows, hard to play") → reduced via larger `maxPathLen` for 18–20; (2) a wide-ellipse spade body and a single-shared-taper 5-spike star/crown both looked better in isolation but were near-zero-solvability dead ends — replaced with proven-solvable bodies plus a distinctive flared stem (spade) and individually-tapered spikes with consistent gaps (crown), shrinking the crown's solid rim band specifically once a large near-rectangular base region turned out to be the solvability bottleneck; (3) star replaced outright with crown per user request (same "card/game" thematic family, not a star). Final: heart=27, diamond=37, club=37, spade=37, crown=28 arrows; all pass `comp=1`, `solvable=true`, `gapExit=Y(figure-ok)`. `manual_levels_test.dart` updated for hardLength/number-range literals (15→20); all other generic tests covered 16–20 with zero changes. 122 tests green, analyze clean, `--validate-only` ALL VALID: true. |
| P15.1 | 2026-06-24 | Done (manual device validation pending) | `lib/features/audio/infrastructure/audio_manager.dart` | Follow-up to P15. User reported music kept playing after backgrounding the app (home button / switching apps) — `AudioManager` had no app-visibility awareness. **Fix:** `AudioManager` now `extends WidgetsBindingObserver`, registers itself in its private constructor, and overrides `didChangeAppLifecycleState`: `paused` → stop music (guarded by new `_musicPausedForBackground` flag, only if a screen currently holds a music claim); `resumed` → restart music automatically, only if it was paused for background *and* a claim is still held. Kept deliberately separate from P15's `_musicClaims` refcount (claims = screen ownership; this flag = OS visibility) so the active `GameScreen` doesn't need to do anything on resume. Only `paused`/`resumed` handled — `inactive` (transient OS state, e.g. notification shade) intentionally ignored to avoid stutter on non-backgrounding transitions. 122 tests green, analyze clean. No new tests — `AppLifecycleState` transitions aren't simulated by this project's widget tests; manual on-device check still pending (background during a level → music stops; foreground again → resumes on its own). |
| P17 | 2026-06-23/24 | Done (manual validation pending) | `lib/core/theme/app_theme.dart`, `lib/features/game/presentation/widgets/graph_board_painter.dart`, `lib/features/game/presentation/widgets/graph_board_layout.dart`, `lib/features/game/presentation/widgets/graph_board_hit_tester.dart`, `lib/features/game/presentation/widgets/graph_board.dart` | Board rendering polish, branch `feat/frontend-rendering` (merged via PR #4, #5). 5-color neon arrow palette replacing 4-color pastel. Node visibility now coverage-based: nodes covered by an active arrow render almost invisible (alpha 0.08), nodes freed by an escaped arrow get a lighter/translucent halo+dot (alpha 0.16/0.5) instead of the old always-opaque white circle. `GraphBoardLayout` exposes `step` (px per cell); stroke width and arrowhead length/width are capped relative to it so dense boards never draw an arrowhead tip over a neighbouring arrow (no-op above ~43px cell size, i.e. unaffected for most of levels 1–15). `GraphBoardHitTester.hitSlop` scaled the same way (cap 45% of cell spacing, floor 12px, ceiling unchanged at 28px) so dense-board taps stop matching the wrong adjacent arrow. `GraphBoard`'s `AspectRatio` now matches the level's own bounding-box aspect (clamped `[0.6,1.6]`) instead of always forcing a square. 122 tests green, analyze clean. No new tests — presentation-only visual/interaction tuning not asserted at pixel level by this project's widget tests. |
| P18 | 2026-06-24 | Pending commit | `lib/features/game/presentation/widgets/graph_board.dart`, `lib/features/game/presentation/game_screen.dart` | Pinch-to-zoom reliability fix, follow-up to P17. **Root cause:** `GraphBoard`'s `InteractiveViewer` is nested inside the page-level `ListView` (`game_screen.dart`'s `_GameReadyView`); Flutter's gesture arena let the ancestor `ListView`'s drag recognizer claim the first finger before the second landed, so `InteractiveViewer`'s scale recognizer rarely got a clean two-finger gesture — a known Flutter `InteractiveViewer`-nested-in-`Scrollable` issue. **Fix:** `GraphBoard` tracks active pointer count via a `Listener` and reports via a new `onInteractionActiveChanged` callback; `GameScreen` uses it to set the page `ListView`'s `physics` to `NeverScrollableScrollPhysics` while any finger touches the board (back to `ClampingScrollPhysics` once all lift), so the ancestor never competes for those pointers. 122 tests green, analyze clean. No new tests — multi-touch gesture-arena races aren't reproducible via this project's synthetic-pointer widget tests. **Uncommitted at time of writing** — working tree was observed to be cleared of this diff twice during the session (external git activity, not this agent) before settling; commit before ending the session. |
| P19 | 2026-07-09 | COMPLETE | `tool/gen_levels.js`, `lib/features/game/presentation/widgets/graph_board_hit_tester.dart`, `assets/levels/manual_levels.json` (16-20 only), `test/features/game/infrastructure/manual_levels_test.dart`, `test/features/game/presentation/graph_board_hit_tester_test.dart` | Audit-first level/rendering validation, branch `feat/phase-19-level-audit-and-validation`. **Found:** figure levels 16-20's blanket exemption from `hasInteriorGapExit` hid 5 real P14-class gap-exit defects (18×1, 19×1, 20×3 — crown worst); `GraphBoardHitTester`'s `minHitSlop=12` floor exceeded `cellSize*0.45` on all 5 figure levels (steps ~15.3-20.3px), violating the "never reach halfway to neighbour" invariant (worst on crown, 56% over cap). Self-intersection (0/166), arrowhead-overlap, and `AspectRatio` clamp all independently verified clean. **Fix:** added figure-aware `hasRealInteriorGapExit(dj)` (only flags a gap when the sweep hits another arrow's node past it, not a harmless silhouette concavity); wired into both `generateFigureLevel` and `validateAll`; raised `FIGURE_MAX_RETRIES` 20,000→100,000 (quantified exhaustion risk was ~5%/1.8% for spade/crown at 20,000 under the stricter check); regenerated levels 16-20 only (1-15 verified byte-identical, took 1,026-4,518 attempts each). Lowered `minHitSlop` 12→6px. 124 tests green (2 new), analyze clean, `--validate-only` ALL VALID: true. |
| P20 | 2026-07-09 | COMPLETE | `lib/features/home/presentation/home_screen.dart`, `lib/core/localization/l10n/app_en.arb`, `lib/core/localization/l10n/app_es.arb`, `android/app/src/main/AndroidManifest.xml` | Main menu redesign + rebrand, branch `feat/phase-20-main-menu-redesign`. Renamed "Arrow POC" → **Nodus** (Latin "knot" — on-theme for a graph-untangling game, not generic) in both ARB `appTitle` values; `MaterialApp.onGenerateTitle` already reads this key so no separate title constant existed to change. `homeSubtitle` replaced with a short tagline in both locales. `HomeScreen` rewritten as a `StatefulWidget`: a `CustomPainter` background (`_MenuBackgroundPainter`) drifts 4 soft neon-tinted blurred glows in slow circular orbits over `AppTheme.background` via one looping `AnimationController` (18s, pure geometry, no images/shaders/physics); large gradient-masked (`neonMint`→`neonBlue`) display title; Play/Settings replaced with a shared `_MenuButton` (tap-scale + glow shadow feedback, filled for Play/outlined for Settings, using existing neon palette colors as accents — no new colors introduced); backend URL demoted into a small `_DebugRow` at 50% opacity, 11px font, near the bottom. All existing localization keys/routes/nav args untouched; no new dependencies; no new assets. `android:label` updated `frontend_poc_arrow`→`Nodus`; no `ios/` directory exists in this project (nothing to update there). 124/124 tests green (0 new — pre-existing tests match by localized text, unaffected by the visual rewrite), analyze clean. |
| P21 | 2026-07-09 | COMPLETE | `frontend-poc-arrow/lib/features/settings/presentation/{settings_screen.dart,settings_screen_controller.dart}`, `frontend-poc-arrow/lib/features/progress/application/{remote_progress_repository.dart,reset_remote_progress_use_case.dart}`, `frontend-poc-arrow/lib/features/progress/infrastructure/{api_remote_progress_repository.dart,local_progress_dependencies.dart}`, `frontend-poc-arrow/lib/core/network/{api_client.dart,http_api_client.dart}`, `frontend-poc-arrow/lib/features/game/presentation/game_ui_keys.dart`, `frontend-poc-arrow/lib/core/localization/l10n/*`, `backend-poc-arrow/src/{application/progress/reset-progress.use-case.ts,application/ports/progress.repository.ts,infrastructure/repositories/prisma-progress.repository.ts,interfaces/http/progress/progress.controller.ts,modules/progress.module.ts}` | Backend progress reset, branch `feat/phase-21-backend-progress-reset`. **Problem:** "Reset local progress" only cleared `SharedPreferences`; returning to level selection re-synced remote progress from the backend and restored unlocked levels for signed-in users. **Backend:** added `DELETE /progress` (204, `JwtAuthGuard`, 401 if unauthenticated) — `ProgressRepository.deleteByUserId`, `PrismaProgressRepository.deleteByUserId` (`deleteMany({where:{userId}})`), new `ResetProgressUseCase`, wired into `progress.module.ts` and the controller. **Frontend:** `ApiClient`/`HttpApiClient` gained `delete()`; `RemoteProgressRepository.resetProgress()` + `ApiRemoteProgressRepository` impl call `DELETE /progress`; new `ResetRemoteProgressUseCase` (application layer) calls remote reset then local reset only on remote success — local is never touched on a thrown exception unless the controller explicitly does so for the offline path. `SettingsScreenController.resetRemoteProgress()` returns a `RemoteResetResult` enum (`success`/`offline`/`unauthenticated`/`failed`), distinguishing offline (network-level `ApiException` with `statusCode == null` → also clears local) from 401 (`unauthenticated`, no local clear) from any other status (`failed`, no local clear). `SettingsScreen` adds a "Reset remote progress" card matching existing styling, confirm dialog, and a snackbar per result; hidden behind a login-required message when logged out. 3 new ARB keys added to both locales and regenerated via `flutter gen-l10n`. **Tests:** 5 new Dart unit tests (success/offline/401/logged-out/generic-failure) — 129/129 frontend tests green, analyze clean, `--validate-only` unaffected (ALL VALID: true, no level files touched). 2 new backend e2e tests (`should_delete_progress_when_user_is_authenticated`, `should_reject_progress_reset_when_unauthenticated`) — 10/10 e2e + 9/9 unit backend tests green, `tsc --noEmit` clean. |
| P21.1 | 2026-07-09 | COMPLETE | `frontend-poc-arrow/lib/features/progress/application/{local_progress_repository.dart,sync_progress_on_login_use_case.dart}`, `frontend-poc-arrow/lib/features/progress/infrastructure/{shared_preferences_local_progress_repository.dart,local_progress_dependencies.dart}`, `frontend-poc-arrow/lib/features/auth/presentation/{auth_screen_controller.dart,auth_screen.dart}`, `frontend-poc-arrow/lib/features/home/presentation/home_screen.dart`, `frontend-poc-arrow/lib/core/localization/l10n/*`, `frontend-poc-arrow/test/{widget_test.dart,features/settings/settings_test.dart,features/progress/local_progress_test.dart}` | Main menu bottom nav + login progress sync, branch `feat/phase-21-backend-progress-reset` (extends P21, uncommitted together). **Task A (login sync):** `LocalProgressRepository` gained `getLastSyncedUserId()`/`setLastSyncedUserId()` (new `progress.lastSyncedUserId` SharedPreferences key). New `SyncProgressOnLoginUseCase` composes this with the existing `SyncProgressUseCase`: if the stored last-synced user id differs from the newly logged-in user's id, local progress is cleared before syncing (prevents user A's local unlocks leaking into user B's account on a shared device); if it matches or is null (anonymous/guest), the existing merge policy runs unchanged (preserves "guest progress merges into new account" path). Wired via `LocalProgressDependencies.createSyncProgressOnLoginUseCase()`, injected into `AuthScreenController` as an optional `syncProgressOnLogin` callback invoked after a successful login/register, wrapped in try/catch so a sync failure never blocks login (non-fatal, matches existing settings-screen sync UX). No backend changes were required — achieved entirely with the existing `GET /progress/me` + merge machinery. **Task B (bottom nav):** `HomeScreen`'s two stacked Play/Settings buttons replaced with a 4-item bottom row (`_MenuNavButton`, icon+label, reused neon styling): Levels, Leaderboard (no level arg → global board), Settings, and a disabled "Game Mode" placeholder (`onPressed: null`, reserved for a future 3D mode) — Nodus wordmark, animated background, and the de-emphasized backend-URL debug row all kept in their P20 positions/placement. Only `gameMode` was a genuinely new ARB key (`levels`/`leaderboard`/`play`/`settings` already existed); added to both locales and regenerated via `flutter gen-l10n`. **Tests:** updated `widget_test.dart`'s two home-screen tests for the new button set/labels; updated 2 existing `LocalProgressRepository` test fakes for the 2 new interface methods; no new dedicated tests for the login-identity-switch logic (would need `AuthScreenController` test coverage, currently untested pre-existing — flagged as a gap, not added this phase). 129/129 frontend tests green, analyze clean, `--validate-only` unaffected (ALL VALID: true, no level files touched). |
| P21.2 | 2026-07-09 | COMPLETE | `frontend-poc-arrow/lib/features/leaderboard/presentation/leaderboard_level_picker_screen.dart` (new), `frontend-poc-arrow/lib/core/routing/app_routes.dart`, `frontend-poc-arrow/lib/features/home/presentation/home_screen.dart`, `frontend-poc-arrow/test/features/game/presentation/playable_game_ui_test.dart` | Leaderboard fix + progress-save regression coverage, branch `feat/phase-21-backend-progress-reset` (extends P21/P21.1, uncommitted together). **Task A (root cause):** the P21.1 main-menu "Leaderboard" button pushed `AppRoutes.leaderboard` with no argument; `LeaderboardScreen._loadEntries()` hard-returns `[]` whenever `levelNumber == null`, so the screen always rendered empty regardless of backend state. The backend exposes only `GET /leaderboard/:levelId` — no global/all-levels endpoint — so a true aggregate board is not achievable frontend-only. **Fix (no backend change):** new `LeaderboardLevelPickerScreen` (route `AppRoutes.leaderboardLevelPicker`) lists all levels via the existing `LocalLevelDependencies` and navigates to the unchanged per-level `LeaderboardScreen(levelNumber: n)` on tap; `HomeScreen`'s Leaderboard button now targets the picker route instead of `AppRoutes.leaderboard` directly. `LeaderboardScreen`/`GetLeaderboardUseCase`/`ApiLeaderboardRepository` untouched — they already worked correctly once given a valid level number (verified during audit). **Task B (root cause):** the reported symptom did not reproduce — `GameScreenController.activateArrow` already invokes `_saveCompletionOnce` synchronously on the `GameStatus.victory` transition (idempotent via `_completionSaved`), independent of the "Next Level" button; the save is a fire-and-forget `SharedPreferences` write untied to widget lifecycle. No production code changed for Task B. **Added regression coverage instead:** 3 new widget tests in `playable_game_ui_test.dart` — save+remote-notify fire exactly once on the victory transition itself (before any overlay button is tapped); repeated taps on victory-overlay elements (Retry) do not re-trigger the save; and the save is already recorded before the player backs out via the app-bar back button without ever tapping "Next Level" (level-selection screen reflects it on return). `_TestGameApp`/`_TestManualLevelsApp` test harnesses extended with injectable `saveLevelCompletion`/`onSaveLevelCompletion` and a `level1Override` for deterministic single-tap victories. No backend changes required for either task. 132/132 frontend tests green (3 new), analyze clean. |

| P22 | 2026-07-09 | Done (manual device validation performed by user; 3D board confirmed working) | `lib/features/game/domain/{board_coordinate,move_direction (new),layer_direction (new),direction,arrow_path,level_definition,level_definition_validator,board_graph}.dart`, `lib/features/game/presentation/widgets/graph_3d_{projector,board_painter,hit_tester,board}.dart` (all new), `lib/features/game/presentation/game_screen.dart`, `lib/core/config/app_config.dart`, `lib/core/routing/app_routes.dart`, `tool/gen_levels.js`, `assets/levels/manual_levels.json` | 3D graph extension on branch `feat/3d-levels`: `BoardCoordinate.z` (default 0 — 2D is the z=0 embedding, no coordinate subclassing to keep map-key equality symmetric); `MoveDirection` interface implemented by planar `Direction` enum (untouched values) + new `LayerDirection.above/below`; type-widening only elsewhere; **`MovementResolver` unchanged** (coordinate sweep was already dimension-agnostic). True-3D presentation: `Graph3DProjector` (yaw/pitch orbit + perspective divide, per-node depth + pixelScale), depth-sorted painter (painter's algorithm, foreshortening, vertical arrows render as real arrows between layers), screen-space hit tester through the same projector, `Graph3DBoard` widget (drag orbits, pinch zooms, initial tilted camera); selected via `boardGraph.isMultiLayer` — 2D `GraphBoard` untouched. Levels 21–22 (deterministic `--generate-3d` mode keeps 1–20 byte-identical). First Phase-D attempt (flat layer-switcher) rejected by user — true rotatable perspective required. Mid-session merge conflict with P19 (PR #18) resolved by combining figure-aware gap check with 3D changes. 160 tests green. |
| P22.1 | 2026-07-09 | Done (figures visually confirmed by user after reshape) | `tool/gen_levels.js`, `assets/levels/manual_levels.json` (21–25), `lib/core/config/app_config.dart`, `test/features/game/infrastructure/manual_levels_test.dart`, `test/features/game/presentation/graph_3d_board_test.dart`, `docs/LEVEL_AUTHORING.md` §16 | **No single-node arrows anywhere** (user feedback: one-cell verticals render as floating dots): verticals always span a z-edge; `verticalSingle` deleted; JS `structureErrors` strict again; new Dart test `should_have_no_single_node_arrows`. Levels 21–22 redesigned around spanning verticals; figure levels 23–25 added, then reshaped once after user feedback ("don't look like the figures"): 23 Pyramid 2×2/4×4/6×6/8×8 (42 arrows), 24 Diamond 2×2/4×4/6×6/4×4/2×2 (34), 25 Hourglass 5×5/3×3/1×1/3×3/5×5 (30, single-cell waist). Interior-gap rule for 3D uses real-gap semantics (JS `hasRealInteriorGapExit3D`; Dart test branches on `generationType == '3d'`) because sweeping past a smaller tier's silhouette is legitimate. `manualLevelCount` = 25. 161 tests green, analyze clean, `--validate-only` ALL VALID (25 levels). |

| P23 | 2026-07-10 | COMPLETE | `lib/features/game/presentation/{game_screen_controller.dart,game_screen.dart}`, `test/features/game/presentation/playable_game_ui_test.dart`, `test/features/leaderboard/presentation/leaderboard_level_picker_screen_test.dart` (new) | Bug fixes & polish, branch `feat/phase-23-bug-fixes-and-polish`. **Task A (save/reload race hardening):** the completion save was fire-and-forget, so an extremely fast pop (system/app-bar back) could race the level-selection screen's post-return progress reload — self-correcting on the next visit, no data loss, but the level could show as not-completed for one return. Fix (Option 1): `GameScreenController` retains the in-flight local save as `_pendingCompletionSave` (assigned only around the local `SaveLevelCompletionUseCase` call, not the best-effort remote notify) and exposes `Future<void> get completionSettled`, a no-op resolved future when no victory has occurred; `GameScreen`'s body is wrapped in `PopScope(canPop: false)` whose `onPopInvokedWithResult` awaits `completionSettled` (guarding `mounted`) before performing the actual pop — covers the app-bar back arrow and the Android system back button (both route through the same pop), leaving `dispose()` untouched (never awaited there). **Task B (leaderboard picker test coverage):** new `leaderboard_level_picker_screen_test.dart` — 5 tests covering render (one card per level with name/number), tap→`Navigator.pushNamed(AppRoutes.leaderboard, arguments: n)` argument capture via a fake `onGenerateRoute`, the empty-list branch, the throwing branch (both assert the localized `leaderboardUnavailable` message and no cards), and the loading spinner before the future settles. 1 new regression test added to `playable_game_ui_test.dart` using a 50ms-delayed fake save to force and assert the settle-before-reload ordering. No backend, gameplay, rendering, or level-data changes. 178/178 tests green (6 new: 1 Task A + 5 Task B), analyze clean, `--validate-only` not run (no level files touched). |

## Notes

- "Date" column: fill in when the phase is executed.
- "Files Touched" lists key files only; see `docs/CODEX_HANDOFF.md` for the full list per phase.
- Update this table at the end of each phase using `harness/templates/handoff_update_template.md`.
